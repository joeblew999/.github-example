name: mise CI
# Generated by `mise run mise:repo:bootstrap`. Thin stub — the reusable workflow
# holds the logic; edit the with: values to control THIS repo's task/matrix.
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: joeblew999/.github/.github/workflows/reusable-mise-ci.yml@main
    with:
      task: ci
      os-matrix: '["ubuntu-latest", "macos-latest", "windows-latest"]'
      sccache: false
