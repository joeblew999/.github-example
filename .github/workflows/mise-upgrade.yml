name: mise upgrade

# Weekly PR that bumps pinned tool versions + joeblew999/.github ref pins.
# Generated by `mise run mise:repo:bootstrap` — do not hand-edit; re-run to update.
on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  upgrade:
    uses: joeblew999/.github/.github/workflows/reusable-mise-upgrade.yml@main
